#
# $Id$
#
# Defines the components that make up this model and its animations.

standing.meshes = head, artillery, gun

walking.meshes = head, artillery, gun

head.texture = head.png
artillery.texture = artillery.png
gun.texture = gun.png
